services:
  - docker:bind

variables:
  DOCKER_HOST: tcp://docker:2375
  DOCKER_DRIVER: overlay

build:
  image: maven:3-jdk-8
  stage: build
  script:
    - mvn clean install
  artifacts:
    paths:
      - target/*.jar